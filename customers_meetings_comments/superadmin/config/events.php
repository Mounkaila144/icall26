<?php

return array(
        "meetings.import.from.site"=>array(
                    "meetings.import.from.site.comments"=>array("CustomerMeetingCommentEvents","importMeetings"),
                             ),   
    
         "site.initialization.execute"=>array(
                "customers.meetings.comments.initialization.execute"=>array("CustomerMeetingCommentEvents","initializationExecute"),
        )
    );