<?php

class mfFilterConfigHandler { 

	function execute($configFiles);
	protected function addFilter($category, $class, $parameters);
	protected function addSecurityFilter($category, $class, $parameters);
}
